구조 설명: "포인터를 사용할 수 없는 환경을 가정하여, 배열 인덱스를 이용한 정적 연결 리스트를 구현함."

핵심 로직: "사용 중인 리스트(Used List)와 빈 노드 리스트(Free List)를 동시에 관리하여 메모리 재사용성을 극대화함."

실행 로그: 타겟 A, B, C를 추가하고 중간의 B를 삭제했을 때, A가 C를 가리키게 되는 터미널 화면 복사.
